# 首页广告浮窗自动播放 — 项目进度交接
整理时间：2026-08-22　|　用于新对话窗口开场上下文

---

## 一、背景

LIAPLIAS 首页（EN 版 `index.html`）新增了一个"Partner Network 广告"模块——展示 LIAPLIAS 认证的合作制造商，用视频形式呈现。这个模块经历了两轮方案调整：

1. **第一版（已废弃）**：首页正文里插入固定的卡片网格区块（Partner Network Showcase），点击卡片跳转到独立详情页 `/en/showcase/partner-0N.html`
2. **第二版（当前方案）**：改成右下角**浮动视频入口**，不占用首页正文版面；点击后弹出居中弹窗，弹窗内可切换查看最多 4 个广告位的视频

详情页 `/en/showcase/partner-01.html` ~ `partner-04.html`（4个文件）之前已经生成过模板（内容为占位符，等真实素材），这块跟本次改动无关，不需要重新处理。

---

## 二、已锁定的技术方案

### 4 个广告位管理机制
- 用 JS 数组 `PARTNER_VIDEOS`（下标 0-3）管理，每个对象含 `active`（是否启用）/`name`/`blurb`/`video`/`poster`/`link`
- 启用某个位置：把对应对象 `active` 改成 `true`，填入真实内容
- 停用某个位置：`active` 改回 `false`
- 当前状态（示例）：位置2、位置3（下标1、2）`active:true`；位置1、位置4（下标0、3）`active:false`（预留）

### 浮动入口自动播放（本次改动核心）
- 浏览器强制规定"自动播放的视频必须静音"，这是全平台统一限制，无法绕开
- 方案：右下角浮动入口做成 **200×112px 的迷你视频预览窗**，页面加载后自动**静音循环播放** `PARTNER_VIDEOS` 中第一个 `active:true` 的视频
- 点击这个预览窗 → 展开成完整弹窗（`vm-overlay`/`vm-box`），弹窗内视频**带声音、带播放控制条**（因为是用户主动点击触发，不受静音限制），底部有 "Partner 1/2/3/4" 标签可切换查看其他启用位置的视频
- **已修复的风险点**：如果 `PARTNER_VIDEOS` 里所有 `active` 都是 `false`（没有任何广告客户在投放），浮动入口需要**整个隐藏**，不能显示空白预览框——用 `fab.style.display='none'` 处理

### 具体代码改动（3处，均为增量补丁，不是整篇重写）
1. **CSS**：`.video-fab` 样式类从"图标+文字胶囊按钮"改造成"视频预览小窗"（新增 `.video-fab video`/`.video-fab-label`/`.video-fab-mute`，移除 `.video-fab-ic`/`.video-fab-txt`）
2. **HTML**：`<div class="video-fab" onclick="openVideoModal()">` 内部结构从图标+文字，换成 `<video id="fabVideo" autoplay muted loop playsinline>` + 静音图标 + 文字标签
3. **JS**：新增 `initFabPreview()` 函数（在 `PARTNER_VIDEOS` 数组定义之后、`buildVmTabs()` 函数之前插入），页面 `DOMContentLoaded` 时调用，找到第一个 `active:true` 的位置填入预览视频；找不到就隐藏整个浮动入口

---

## 三、⚠️ 需要新窗口先解决的文件版本冲突

**当前问题**：Claude 这边保留的工作文件版本，和用户本地实际维护的文件版本，出现了不同步：

- **Claude 侧工作文件**：已经应用了"浮动视频自动播放"这三处改动，但**首页分类导航（Browse by category）部分还是旧的硬编码版本**（6条写死的 `<a>` 链接）
- **用户本地文件**：分类导航已经**手动改成了动态加载**（`fetch('/assets/data/category-mapping.json')` 从 JSON 拉取分类列表并动态生成链接），但**没有**这次的浮动视频自动播放改动

也就是说，两边各自更新了对方没有的部分，需要**以用户本地文件为准，合并进浮动视频这部分改动**。

---

## 四、建议新窗口开场直接这样说

"继续 LIAPLIAS 首页广告浮窗自动播放的工作，已有进度见上传的这份文档，同时上传我本地最新的英文版 index.html（已包含分类导航动态加载）"

并同时上传：
1. 本文件
2. 你本地最新的英文版 `index.html`

新窗口里我会：
1. 先确认上传文件里分类导航确实是动态加载版本
2. 把本文档第二节里的 3 处浮动视频自动播放改动，套用到你上传的这份最新文件上
3. 只输出改动部分的代码 + 精确位置（不整篇重新输出），除非你需要我直接生成可下载的成品文件

---

## 五、工作方式约定（延续自其他 LIAPLIAS 项目线程）

- 改代码前必须先看真实代码，不能凭结构猜测
- 代码类改动，只给增量部分+精确位置，不整篇重复输出（除非明确要成品文件）
- 技术判断/实现方案类问题由 Claude 直接决策，做完汇报"做了什么/为什么"；业务/产品呈现/翻译内容由用户决策
- 大文件/长对话注意 token 消耗，适时切换新窗口
